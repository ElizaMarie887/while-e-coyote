const prompt = require('prompt-sync')({sigint: true});

let number = Number(prompt("Enter a starting number (less than 100): "))

let x = 0

while(number <= 100){
    if(number *= 2)
    console.log (number)
}